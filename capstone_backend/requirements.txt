Flask==3.0.3
flask-cors==4.0.0
pymongo==4.8.0

transformers==4.44.0
torch==2.2.2

pytesseract==0.3.13
pdf2image==1.17.0
Pillow==10.4.0

datasets==2.19.1
rouge-score==0.1.2
bert-score==0.3.13

numpy==1.26.4
tqdm==4.66.4
scikit-learn==1.5.0

sentence-transformers==2.7.0
sentencepiece==0.2.0

# Vector database for the clause knowledge base (spec section 5).
# faiss-cpu backs kiran_retrieval.vector_index. Without it that module falls back to
# a pure-Python inner-product scan behind the same API: correct, but O(n) per query,
# so install it for anything past a demo corpus.
faiss-cpu==1.8.0

python-dotenv==1.0.1
pytest==8.3.2
